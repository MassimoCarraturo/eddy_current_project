% Field-level equilibrium: the per-column free-surface closure is exact at the
% traction-free top but degrades with depth, where sigma_zz no longer vanishes.
% The mlhp hp-FEM solve recovers eps_zz at every depth (it is the true profile).
\documentclass[border=2pt]{standalone}
\input{tikz-common.tex}
\begin{document}
\begin{tikzpicture}
\begin{groupplot}[
    group style={group size=2 by 1, horizontal sep=15mm},
    width=60mm, height=50mm,
    xlabel={build height (norm.)},
  ]
  % ---- panel 1: eps_zz profile, true (FE) vs surface closure ----
  \nextgroupplot[
      ylabel={$\varepsilon_{zz}$ (\si{\percent})},
      legend pos=north west, legend style={font=\scriptsize},
    ]
    \addplot[black, line width=0.9pt]
      table[x=z, y=true_zz, col sep=comma] {../data/eq_field_profile.csv};
    \addlegendentry{FE equilibrium (true)}
    \addplot[only marks, oiVermillion, mark=x, mark size=1.6pt]
      table[x=z, y=closure_zz, col sep=comma] {../data/eq_field_profile.csv};
    \addlegendentry{surface closure}
    \draw[gray, densely dotted] (axis cs:0,0) -- (axis cs:1,0);
  % ---- panel 2: why -- normalised sigma_zz vanishes only at the free top ----
  \nextgroupplot[
      ylabel={$\sigma_{zz}/\max|\sigma_{zz}|$},
      legend pos=north east, legend style={font=\scriptsize},
    ]
    \addplot[oiBlue, line width=0.9pt]
      table[x=z, y=sigma_zz_norm, col sep=comma] {../data/eq_field_profile.csv};
    \addlegendentry{$\sigma_{zz}$ (FE)}
    \draw[gray, densely dotted] (axis cs:0,0) -- (axis cs:1,0);
    \node[anchor=north east, font=\scriptsize, gray] at (rel axis cs:0.97,0.97)
      {free top $\to 0$};
\end{groupplot}
\end{tikzpicture}
\end{document}
