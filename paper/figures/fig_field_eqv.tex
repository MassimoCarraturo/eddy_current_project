% Reconstructed von-Mises-equivalent strain field over the build.
\documentclass[border=2pt]{standalone}
\input{tikz-common.tex}
\begin{document}
\begin{tikzpicture}
\begin{axis}[
    width=70mm, height=52mm,
    xlabel={$x$ (norm.)},
    ylabel={build height (norm.)},
    enlargelimits=false, axis on top, grid=none,
    view={0}{90},
    colormap/viridis,
    colorbar,
    colorbar style={ylabel={$\varepsilon_{\mathrm{eq}}$ (\%)}, ylabel style={font=\footnotesize}},
  ]
  \addplot3[surf, shader=interp] table[x=x, y=y, z=z] {../data/field_eqv.dat};
\end{axis}
\end{tikzpicture}
\end{document}
